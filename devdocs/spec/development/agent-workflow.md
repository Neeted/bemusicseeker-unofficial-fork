# エージェントによる開発作業

## 目的と適用範囲

複数段階の変更で、要件整理、必要なテスト設計、実装、検証、レビューの責任を分けます。単純な質問や軽微な文書修正に、機械的な委譲は要求しません。各機能の挙動は機能仕様、テストの期待結果は[テスト作成](test-authoring.md)、実行条件は[テスト検証](testing.md)を正本とします。

## 用語

共通の語義は[用語集](../glossary.md)を参照します。本書の「ルート」は利用者の依頼を受けて作業全体に責任を持つエージェント、「担当」はルートが範囲を指定して委譲したエージェントです。「凍結した変更」は、レビュー中に誰も変更しない作業ツリーを指します。

## 仕様

### 役割と順序

| 役割 | 担当すること | 担当しないこと |
| --- | --- | --- |
| ルート | 要件・設計の確定、テスト必要性の判断と設計承認、担当範囲の割当、統合、最終検証・報告 | 未決の仕様判断を実装担当へ押し付けない。 |
| `plan-clarifier` | 計画案の不明点、前提、テスト設計の要否、並列境界を読取り専用で点検 | 実装、期待結果の確定、検証実行、別担当の起動。 |
| `test-contract-designer` | 実装から独立した判定基準を先に定め、その後で既存テストへの配置を設計 | 実装、検証実行、作業ツリーの編集、別担当の起動。 |
| `implementation-worker` | 確定した一作業単位を、指定されたパスと承認済みの期待結果の範囲で実装 | 仕様の再決定、全体統合、無断の範囲拡大。 |
| `issue-resolver` | 実装担当が発見した重大な障害を、承認済みの契約と指定パス内で解決 | 通常実装の代行、広範な再設計、別担当の起動。 |
| `repo-static-review` | 凍結した変更と関連経路を読取り専用でレビュー | 編集、検証実行、アプリ起動、別担当の起動。 |

原則の順序は、ルートの計画案、計画点検、必要な独立テスト設計、ルートの承認、実装、統合検証、凍結した変更の独立レビューです。計画点検は着手前に一度行い、重要な前提が変わった場合だけやり直します。テスト設計担当は、その作業の実装担当を開始する前に終了します。

### 要件と到達可能性

ルートは目的、背景、制約、対象外、完了条件、決定事項、担当範囲、検証方法を揃え、実装担当が挙動を推測せずに着手できる状態にします。実行時の不具合・失敗を対象にする場合は、本番の入口、そこで成立する前提、管理主体までの経路、利用者・永続データ・外部データへの影響を示します。

非公開メソッドの直接呼出し、リフレクション、テスト用の代替実装だけで作れる状態は、本番からの到達の証明にはなりません。承認された新機能の入口は、計画で処理経路と影響を示せば対象にできます。入力時点で防げる不正状態は、後段の復旧処理よりも、責任を持つ最初の入口での検証・明示的な拒否を優先します。

利用者に見える結果、保存、互換性、責務、失敗の扱いが一意に決まらない場合だけ、選択肢・推奨案・影響をルートへ返します。既に承認され、リポジトリから確認できる事項の再承認は求めません。検証の失敗や差分競合は、本番からの到達ではなく実際の失敗記録・競合箇所で判断します。

### テスト設計の要否

ルートは変更の目的と影響から、恒久テストを「変更なし」「既存更新」「追加」「削除」のいずれとして扱うか決めます。追加・判定内容の意味変更・置換が必要な範囲だけ、[テスト作成](test-authoring.md#仕様の根拠とテスト設計書)に従う独立設計を行います。削除のみで代替不要と判断済みの作業、意味を変えない名前変更・移動・整形には、設計書を要求しません。

設計担当は、先に仕様の根拠から期待結果、許容差分、見逃してはいけない誤実装を確定します。その後に実装や既存テストを読み、到達経路、配置、共有資源、待機・解放方法を確認します。現在の実装出力や既存期待値に合わせて、先に確定した判定基準を変更しません。

### 計画と記録

進行中の[計画](../../plan/README.md)には、現在の判断・残作業・再開に必要な情報だけを置きます。テスト設計書の作業名・項目名は、計画と引継ぎの一時的な参照です。実装順の番号を、恒久仕様・コード・テスト名・コメントへ転記しません。

完了時は、現在も必要な契約と実装・テストの対応を機能仕様、採用理由を設計判断、具体的な残課題を未完了の計画へ引き継ぎます。元の計画・作業専用の設計書・配置表・検証記録は削除し、参照も更新します。通常の作業ログを恒久資料に移しません。承認内容を後で確認する場合は、引継ぎか指定された Git 上の版を使います。

### 並列作業

実装担当は通常一人、同時書込みは最大二人とします。目的・パス・生成物・スキーマ・共有テストの担当が分離し、先行作業の結果に依存しない場合だけ並列化します。同じ機能の別実装を競わせません。

設定上の同時担当数は、入れ子の障害解決担当を含めて最大三人です。二人で実装する場合も、障害解決用の一枠を残します。呼出しの深さは「ルート → 実装担当 → 障害解決担当」までで、点検・設計・レビュー担当はルートが直接呼ぶ末端の役割です。各役割のモデル、推論強度、権限は `.codex/agents/`、全体の上限と待機方針は `.codex/config.toml` を正本とします。

### 実装担当への入力

目的、完了条件、確定した設計、書込み可能パス、既存差分、関連仕様、検証方法を渡します。テストの意味を変更する場合は、承認済み設計書・対象項目・配置・共有資源・完了の待ち方も渡します。必要な仕様判断や設計書が不足する場合は、担当は編集を始めず `NEEDS_ROOT_INPUT` を返します。既存テストや補助処理の候補は、指定範囲から担当自身が調査します。

担当は指定範囲内で実装し、承認された期待結果を保ったままテストの準備・補助処理・表明方法を適合させます。通常の局所修正は自分で解決し、関連する `Quick` などの検証を行います。全体の `Functional`・`Full`・静的レビューはルートへ任せます。ステージ、コミット、送信、タグ、バージョン更新は行いません。

### 障害解決担当を使う条件

通常の局所修正では解消できず、次のいずれかに該当する場合だけ、実装担当から一度呼び出せます。呼出し中は実装担当の編集を止めます。

| 障害 | 必要な根拠 |
| --- | --- |
| データ消失、セキュリティ、起動不能、現実的な競合・停止・互換性の退行 | 本番の入口から影響までの経路と発生条件。 |
| 承認済みの契約と実行可能な境界の技術的な不一致 | 期待結果を変えずに解消する可能性があり、到達可能性と影響が確認済みであること。 |
| 繰り返す検証失敗・時間超過 | [検証規則](testing.md)に沿う結果。真の時間超過では、規定の一度の再実行も失敗したこと。 |
| 実際の差分・担当範囲の競合 | 競合ファイル、双方の担当、機械的な引継ぎだけで解決できない理由。 |

現在の差分、許可パス、維持する条件、必要な設計書、実施した検証、障害の根拠を一組で渡します。障害解決担当は期待結果を変えず、指定パス内の技術的修正だけを行います。仕様、永続データ、互換性、機能の責務、許容差分の変更や、未承認の状態・再試行・復旧機構が必要なら `NEEDS_ROOT_DECISION` を返します。テスト実行・待機・解放の技術的修正は、既存の検証契約を守る範囲で行えます。

### 実装担当の完了報告

得られた結果、主要変更とファイルの役割、実行した検証と結果、統合時の依存・競合・残課題を日本語で要約します。テストを追加・意味変更・置換した場合は、設計項目と実際のテストメソッド、既存テストとの置換関係、共有資源、検証区分、待機・解放方法、必要と判断して行った失敗再現・識別力の確認を添えます。

検証の省略や時間超過があった場合は、適用した指示と実際の結果を区別して引き継ぎます。失敗再現は、コンパイルや準備の失敗ではなく、対象の仕様に反する観測結果を示します。記録の保存範囲は「計画と記録」に従います。

### 統合と独立レビュー

ルートは各担当の差分と引継ぎを確認し、担当の重複、設計との一致、旧経路の退役を確認して統合します。担当が完了した範囲を再実装せず、依存と受入条件の統合に集中します。必要な検証後に実装担当を終了し、作業ツリーを凍結して、新しいレビュー担当へ一度渡します。

レビュー担当には目的、受入条件、変更分類・テスト必要性、比較する版、作業差分、検証要約を渡します。適用したテスト設計書や失敗再現・識別力の確認結果は、該当する場合だけ添えます。レビュー中は、ルートもリポジトリの読取り・検索・編集・検証・整形・VCS操作を止めます。

修正必須の指摘は、重大度、違反する要件、到達経路、発生条件、利用者やデータへの影響、`file:line` 等の根拠、確信度、修正範囲を一組で示します。重大度は、直ちにデータ消失・セキュリティ・起動不能へつながる P0、現実的な誤動作・停止・互換性退行の P1、受入条件に直接反する P2 とします。単なる好み、仮説、到達不能な状態、対象外の改善案は分けます。

期待値の独立性、誤実装との区別、テストの必要性も確認します。失敗再現や一時的な誤実装の実行は、必要と判断した場合だけ受入条件に含めます。完了計画の保存は要求せず、契約・残課題の移管と参照更新を確認します。ファイルとDBの指摘は[整合性のレビュー基準](../library/file-db-consistency.md)も使い、受容済みの限界から新しい復旧保証を導きません。

修正後は関連検証を行い、前回の版、指摘、修正差分を新しいレビュー担当へ渡します。再レビューは修正と直接影響する契約を中心に行い、既確認範囲を無制限に広げません。同じ作業で二回の修正レビュー後も新しい P1 が続く場合は、責務・範囲・完了条件・分割を再計画します。

### 担当機能を利用できない場合

役割専用のエージェントがない場合は、同じモデル・推論強度・権限・役割契約を持つ汎用担当で代替します。複数担当を利用できない場合は、ルートが要件整理、独立した判定基準の設計、実装、検証、レビューを順番に分けます。判定基準の作成前に実装本体・現在出力・既存期待値を根拠にせず、結果では独立レビューと自己点検を区別します。

汎用の作業手順を、対象リポジトリの適用規則や既承認の判断と取り違えません。未決の判断が本当に残る場合だけ、該当指示と適用理由をルートへ返します。

## 実装とテストの対応

| 仕様項目 | 指示・設定 | 確認方法 |
| --- | --- | --- |
| 役割、権限、並列度 | [ルート指示](../../../AGENTS.md)、[全体設定](../../../.codex/config.toml)、[役割設定](../../../.codex/agents/README.md) | TOML構文と機械設定、各役割の入力・禁止事項が本書と一致するかを確認する。 |
| テスト設計と実装の分離 | [テスト配下の指示](../../../BeMusicSeeker.Tests/AGENTS.md)、[テスト作成](test-authoring.md) | 設計を適用する変更か、根拠・期待結果・配置が承認内容と一致するかを点検する。 |
| 計画の退役と引継ぎ | [資料運用](../../README.md)、[計画運用](../../plan/README.md) | 最終差分と参照検索で、契約・残課題の移管、削除、一時名の漏出を確認する。 |
| 統合検証・レビュー | [検証仕様](testing.md)、[レビュー担当](../../../.codex/agents/repo-static-review.toml) | 実際の検証要約、比較版、凍結範囲、指摘と修正の対応を確認する。 |

運用の意味を固定する自動テストは設けず、指示と設定の構文・整合・実際の引継ぎで確認します。

## 関連資料

[テスト作成](test-authoring.md)、[検証](testing.md)、[ワークフローの共通設計](../core/workflow-concurrency.md)、[計画](../../plan/README.md)。
