# エージェントによる開発作業

## 目的と適用範囲

複数段階の変更で、仕様判断、実装、検証、独立レビューを分担します。必要な判断だけを専用担当へ渡し、単純な質問や軽微な修正には機械的な委譲を要求しません。機能の挙動は各機能仕様、テスト設計は[テスト作成](test-authoring.md)、実行条件は[検証](testing.md)を正本とします。

## 用語

「ルート」は依頼を受けて作業全体に責任を持つエージェント、「担当」はルートが範囲を指定して呼ぶエージェントです。「凍結」はレビューの応答完了まで誰も作業ツリーを変更しない状態です。その他は[用語集](../glossary.md)に従います。

## 仕様

### 役割と順序

| 担当 | 責務 |
| --- | --- |
| ルート | 要件・設計の確定、テスト必要性と設計の承認、担当選択、統合、最終検証・報告。 |
| `design-advisor` | 未決の難しい設計問題への助言。承認や実装はしない。 |
| `test-contract-designer` | 仕様から判定基準を先に定め、その後にテスト配置を設計する。 |
| `plan-clarifier` | 完成した実装引継ぎを、未記載の契約を補完せずに読んで質問する。 |
| `implementation-worker` | 承認済み契約と指定パス内で実装・局所検証する。 |
| `issue-resolver` / `critical-issue-resolver` | 通常 / 重大な技術的障害を、契約と指定パスを守って解決する。 |
| `repo-static-review` / `critical-static-review` | 通常 / 重大変更を、同じ合格基準で独立レビューする。 |

順序は、計画案 → 必要な設計相談 → 必要な独立テスト設計と承認 → 引継ぎ点検 → ルートの確定 → 実装 → 統合・検証 → 凍結・独立レビューです。相談・設計・点検担当は実装開始前に終了します。通常用と重大用を毎回順番に呼びません。

### 担当の共通契約

全担当をルートが直接呼び、担当から別担当を起動しません。開始時に `git status --short`、ルートと対象範囲の `AGENTS.md`、本書の共通規則と役割の節を確認します。調査は必要な根拠と関連経路に絞り、同じ全体調査や履歴の複写を繰り返しません。仕様の補助図を扱う際は[補助図の運用](../README.md#補助図mermaid)に従います。

書込みは許可パス内に限り、無関係な既存差分を変更しません。読取り専用担当は編集・検証実行・整形・アプリ起動をしません。全担当ともステージ、コミット、送信、タグ、バージョン更新を行いません。

資料・入力不足は `NEEDS_ROOT_INPUT`、契約・担当範囲の変更判断は `NEEDS_ROOT_DECISION` としてルートへ返し、影響する編集を止めます。利用者に判断を求める必要があるかはルートが決めます。結果は日本語で簡潔に返し、未確認・未実施を成功として報告しません。

### 要件と到達可能性

目的・制約・対象外・完了条件と、挙動・責務・保存・互換性・失敗・取消・性能の契約を確定します。契約内の実装方法は担当の裁量に残し、承認済み事項を再質問しません。

実行時の問題は本番の入口・前提・経路・影響で判断し、テストだけで作れる状態や仮説を根拠に保証を増やしません。承認済み新機能は計画上の入口、検証失敗・競合は実際の診断を根拠にします。

### 重大変更の判定

ルートは計画時と最終差分の確定時に、次の契約または保証の実現方法を実質的に変えるかを確認します。

| 境界 | 対象 |
| --- | --- |
| 永続化・互換性 | 保存、削除、置換、補償、確定、データ形式・互換性。 |
| 並行性・寿命 | 受付、取消、再入、世代、ロック、Dispatcher、終了・解放の順序と所有。 |
| 責務・処理方式 | 複数主体間の責務・可変状態の移管、大規模データの走査・索引・キャッシュ・バッチ化。 |
| 検証・実行制御 | 上記の保証を支えるテストの判定変更・置換・削除、セキュリティ、書込み権限、承認、必須検証、失敗報告の条件。 |

該当する変更は `critical-static-review`、それ以外は `repo-static-review` を選び、理由を計画か引継ぎに一、二文で示します。挙動維持でも実現方法を組み替える変更は対象です。ファイル名・差分量・機械的な改名や整形だけでは判定しません。境界への影響を確認できない場合は、未確認点を示して重大変更として扱います。

### 設計相談

重大な境界の新設・再配分に未決事項がある、または設計案の責務・状態・互換性・失敗・性能を比較する必要がある場合に `design-advisor` を使います。承認済み設計の再確認には使いません。

判断したい問題、維持条件、事実と参照、変更可能範囲、既知の候補を渡します。担当は推奨と理由、削除・統合できる処理や状態、検証観点、未決事項を返します。候補の水増し、実装、テスト設計書の代作、全体レビューはしません。採否はルートが決め、利用者の未決の希望を代わりに承認しません。

### テスト設計の要否

ルートは恒久テストを「変更なし」「既存更新」「追加」「削除」に分類します。追加・意味変更・置換が必要な範囲だけ `test-contract-designer` に渡し、[テスト作成](test-authoring.md#仕様の根拠とテスト設計書)に従う独立した判定基準と配置を承認します。機械的変更と代替不要の削除には設計書を要求しません。初期入力を含む独立性と、判定基準の確定後に実装を調べる順序は同仕様に従います。

### 実装担当への入力

目的・完了条件・対象外、確定した契約と根拠、担当の裁量、許可パス・既存差分・依存と共有資源、関連仕様、検証方法を渡します。必要なテスト設計は承認済み項目・配置・待機と解放を参照します。既存の計画か引継ぎメッセージを使い、専用台帳や重複資料は作りません。

担当は契約を保って実装し、既存の補助処理やテストの準備・表明方法を適合させます。局所修正と関連する `Quick` 等は担当、全体の `Functional`・`Full` はルートが行います。

### 引継ぎの点検

必要なテスト設計の承認後、実装直前に `plan-clarifier` を一度呼びます。実装担当と同じ引継ぎを渡し、計画策定の会話や他担当への権威付けを添えません。起動時の履歴継承も確認し、未記載の前提を持つ状態を独立した読解点検とは扱いません。

担当は適用指示・引継ぎ・明示された参照だけを読み、広範な探索や実装からの仕様推定をしません。結果が変わる不足・矛盾について、該当記述と実装への影響を質問します。参照先に回答があれば根拠を示し、再質問しません。担当の裁量は未決と区別し、推奨案・設計改善・テスト必要性の再判定は担当しません。

必要な参照を確認できなければ `NEEDS_ROOT_INPUT` として点検未完了を返します。「質問なし」は設計の正しさ・網羅性・安全性の保証ではありません。ルートは不足を解消して引継ぎを確定します。説明の補充だけなら再点検せず、契約・範囲・期待結果が実質的に変わる場合だけ、影響する設計・承認と引継ぎ点検をやり直します。

### 障害解決担当を使う条件

局所修正で進めなければ、障害、観測結果、試したこと、維持条件、差分、許可パスをルートへ返します。ルートは情報補充・設計判断と技術的修正を分け、通常の行き詰まりに `issue-resolver`、データ消失・セキュリティ・起動不能・現実的な停止や競合・互換性退行、重要境界をまたぐ難しい障害に `critical-issue-resolver` を直接選びます。

重大用の前に通常用で失敗する必要はありません。真の時間超過は[検証規則](testing.md)の一度の再実行後の結果を使い、呼出しのためだけの追加実行はしません。元の担当の編集停止を確認して書込みを引き継ぎ、解決担当の応答・編集終了まで元の担当とルートは同じ範囲を編集しません。

両担当とも契約・期待結果・許容差分を保つ修正と局所検証だけを行い、続行の可否を返します。契約・責務・範囲の変更や未承認の状態・再試行・復旧機構が必要なら `NEEDS_ROOT_DECISION` を返します。制限時間・並列度・表明で失敗を隠さず、根拠のない障害は修正しません。新しい根拠や担当を替える具体的な理由なく呼出しを繰り返さず、再計画か利用者への判断依頼へ戻します。

### 実装担当の完了報告

実装・障害解決担当は、結果、変更ファイルの役割、検証と結果、依存・競合・残課題を返します。テスト変更は設計項目と実際のテスト、置換関係、共有資源・実行区分・待機と解放を対応付けます。必要と判断した失敗再現・識別力の確認、省略・時間超過は、適用条件と実際の結果を区別して報告します。

### 統合と独立レビュー

ルートは差分・引継ぎから担当の重複、契約との一致、旧経路の退役、文書の移管を確認して統合します。完了範囲を再実装せず、最終検証後に書込み担当を終了して凍結します。初回は当該変更の設計・実装に参加していない新しい担当を、重大変更の判定に従い一人選びます。

目的・受入条件、分類理由、比較版・対象差分、検証要約、必要な承認済みテスト設計を渡します。レビュー中はルートもリポジトリの読取り・検索・編集・検証・整形・VCS操作を止めます。凍結が破られた場合は結果を流用せず、再凍結して影響差分を確認します。

両担当は指定版間と作業ツリーの対象差分（ステージ済み・未ステージ・未追跡）から、関連する呼出元・失敗経路・テスト・仕様を追います。要件充足、正常・空入力・失敗・取消・再入・終了、互換性、責務・スレッド・資源寿命、性能、ログ・翻訳・バージョン、テストの独立性を変更に応じて点検します。[ファイルとDBの整合](../library/file-db-consistency.md)等の受容済みの限界から新しい保証を要求しません。

指摘は重大度、違反要件、経路・条件・影響、`file:line`、確信度、修正範囲を示します。P0は直ちにデータ消失・セキュリティ・起動不能へつながる問題、P1は現実的な誤動作・停止・互換性退行、P2は受入条件への直接違反です。好み・差分量・仮説・対象外の改善は分け、指摘の有無、確認範囲、不確かさを返します。必要範囲の未確認を合格にしません。

通常担当も重大な指摘を報告します。重大変更の見落しや判断しきれない重大問題があれば、指摘・確認済み範囲・未確認点・切替理由をルートへ返します。重大用は最終差分全体を参照し、引継ぎを活用して確認を終えます。通常用の完走や二重の全体レビューは要求しません。

### 再レビューと再計画

レビューの応答完了後に凍結を解除して修正します。修正中はレビュー担当の調査を止め、関連検証後に再凍結し、前回の版・指摘・修正差分・検証結果を渡します。編集に参加していない同じ担当を継続利用でき、修正と直接影響する契約を中心に確認します。重要な前提・範囲・使用担当の変更や、確認履歴を確実に引き継げない場合は新しい担当へ替えます。

二回の修正後も受入を妨げる指摘が解消しない、または同じ領域で新しい重大な契約違反が続く場合は、責務・範囲・完了条件・分割を再計画します。回数や消費量を理由に未解決の指摘を合格にしません。

### 並列作業

書込みは通常一人、最大二人です。目的・パス・生成物・スキーマ・共有テストが分離し、先行作業に依存しない場合だけ並列化します。同じ機能の別実装は競わせません。障害解決は停止した担当の所有を引き継ぎます。

停止・待機中も含む子担当の上限と待機は[全体設定](../../../.codex/config.toml)に従い、不要な担当を終了して解決用の枠を確保します。完了はイベントで待ち、確認だけの状態・会話・差分・ログの反復読取りはしません。

### 計画と記録

[資料運用](../../README.md)と[計画運用](../../plan/README.md)に従い、現在の判断・根拠・残作業・再開情報だけを残します。完了時は必要な契約・設計判断・残課題を移管し、作業専用資料を削除して参照を更新します。一時的な番号やログを恒久資料・コード・テストへ持ち込みません。レビューの根拠は引継ぎか指定されたGit上の版を使い、完了計画の保存を要求しません。

### 担当機能を利用できない場合

専用担当がなければ、同じモデル・推論強度・権限・役割契約を明示した汎用担当を使えます。同等の設定や独立入力を確保できなければ該当工程を未実施として返し、完了扱いにしません。別モデルや自己点検への無断の置換はせず、異なる条件で続行するには相違・影響を示して利用者の合意を得ます。

## 実装とテストの対応

| 仕様項目 | 設定・参照 | 確認方法 |
| --- | --- | --- |
| 選択・権限・並列度 | [役割設定](../../../.codex/agents/README.md)、[全体設定](../../../.codex/config.toml) | TOML構文、モデル・権限、通常 / 重大の選択、ルートからの直接起動。 |
| 引継ぎ・独立設計 | [テスト作成](test-authoring.md) | 初期入力、順序、補完しない質問と裁量の区別、期待結果の根拠。 |
| 所有・レビュー | [検証](testing.md)、[補助図の運用](../README.md#補助図mermaid)、本書 | 書込みの引継ぎ、凍結と再開、補助図の確認、検証結果、指摘と修正、未実施の報告。 |

構文・参照・UTF-8・LF・`git diff --check` に加え、運用変更時は担当の起動・設定適用・入力継承を実機で確認します。運用文言を固定する自動テストや専用の評価基盤は設けません。

## 関連資料

[開発指針](../../../AGENTS.md)、[テスト作成](test-authoring.md)、[検証](testing.md)、[資料運用](../../README.md)。
