# 開発への参加

不具合報告、改善提案、外部からのプルリクエスト（PR）を受け付けます。開発中のコードは `dev`、正式公開済みのコードと配信データは `main` で管理します。アプリを利用する場合は [Releases](https://github.com/Neeted/bemusicseeker-unofficial-fork/releases) の配布パッケージを使用してください。

## 不具合報告・改善提案

[Issues](https://github.com/Neeted/bemusicseeker-unofficial-fork/issues) で既存の報告を確認してから投稿してください。不具合報告には、使用したアプリの版（開発版ならコミット）、Windowsの版、関係する動作モード・設定、再現手順、期待した結果と実際の結果を記載します。ログや画像を添える場合は、個人情報や公開できないデータを除いてください。

改善提案では、解決したい問題と想定する使い方を説明してください。小さな修正は事前のIssueなしでPRを送れます。大きな機能追加・仕様変更は、実装前にIssueで相談してください。

## 変更の準備

1. [開発環境の構築](devdocs/setup.md)に従って環境を準備します。
2. 外部から参加する場合は本リポジトリをforkし、最新の `dev` から作業ブランチを作ります。
3. [AGENTS.md](AGENTS.md)の設計・実装・文書の規則と、変更領域の[開発資料](devdocs/README.md)を確認します。

エディターやIDEは任意です。CodexなどのAIエージェントの利用は必須ではなく、エージェントの委譲・レビュー手順を人による開発に要求するものではありません。

一つのPRは一つの目的にまとめ、無関係な整形や修正を混ぜないでください。挙動を変える場合は、必要な仕様・テスト・利用者向け文書も更新します。ローカルのDB・設定・ログ・配布ZIPなどの作業データは含めません。既に管理している生成物の変更は、その領域の手順に従います。第三者由来のコードやデータを追加する場合は、出典と利用条件を明記してください。

## 検証とPR

通常のPRの宛先は `dev` です。本文には、変更の目的と内容、関連Issue（ある場合）、実行した検証と結果、互換性への影響や注意事項を記載してください。未実施の検証がある場合は、その内容と理由も記載します。

検証はローカルで行い、GitHubのCIは使用しません。標準入口は PowerShell 7 の `scripts/verify-refactor.ps1` です。通常の最終統合は `Functional`、配布・更新・リリースに関わる変更は `Full` を使います。実行条件・回数・失敗時の扱いは[テスト検証](devdocs/spec/development/testing.md)、文書のみの変更は[文書変更の確認](devdocs/README.md#文書変更の確認)に従います。

保守者は、仕様との整合、互換性、性能、保守負担をレビューし、統合する版のローカル検証結果を確認して受け入れを判断します。検証成功だけで採用が決まるわけではなく、修正や範囲の見直しをお願いする場合があります。

## 保守者の開発とIssueの完了

保守者自身の開発は、作業ブランチで変更・検証し、ローカルで `dev` に統合して送信します。Issueへの対応でも自己PRは作りません。ブランチ間の統合、緊急修正、正式公開は[ブランチ運用とリリース手順](devdocs/spec/development/release.md)に従います。

修正対象のIssueは、原則として修正が `dev` に統合された時点で閉じます。関連コミットまたはPRを参照し、正式版が未公開ならその旨をIssueに記載します。完了は正式リリース済みを意味しません。コミットやPR本文の `Fixes #123` で自動クローズする場合も同様です。正式公開まで開いておく必要があるIssueでは、`#123` の参照だけを使い、公開を確認して手動で閉じます。
